{
  "name": "subscription-insights-mcp",
  "version": "1.0.0",
  "description": "Portfolio demo: a workflow-shaped MCP server exposing a SaaS subscription dataset to an LLM (at-risk customers, revenue breakdown, account health). Production-shaped: stderr-only logging, graceful degradation on dirty data, protocol-safe errors, and a held-out eval suite in CI.",
  "type": "module",
  "main": "src/server.js",
  "bin": { "subscription-insights-mcp": "src/server.js" },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/server.e2e.test.js test/tools.test.js",
    "inspector": "npx @modelcontextprotocol/inspector node src/server.js"
  },
  "keywords": ["mcp", "model-context-protocol", "claude", "llm", "ai-agent", "tools"],
  "author": "Matthew Daly",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.0"
  }
}
